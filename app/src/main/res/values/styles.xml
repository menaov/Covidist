<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.CustomTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!--<item name="android:windowBackground">@drawable/add_item_shape</item>-->
    </style>

    <style name="SignInTheme" parent="AppTheme.CustomTheme">
        <item name="android:textColor">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="android:layout_gravity">center|top</item>
        <item name="android:windowBackground">@drawable/world_map_carbon_headline</item>
    </style>
</resources>
